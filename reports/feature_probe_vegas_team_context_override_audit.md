# Vegas team-context override — audit

Generated by `python -m scripts.build_vegas_team_context_override --seasons 2018-2024`
on 2026-05-17.

Output: `data/features_probe/vegas_team_context.parquet` (NOT committed; regenerable).

Total rows: 56,652

## Per-column coverage

| Column | Coverage |
|---|---|
| `preseason_implied_team_total` | 100.00% |
| `preseason_spread` | 100.00% |
| `season_avg_implied_team_total` | 94.13% |
| `season_avg_spread` | 94.13% |

Expected: `preseason_*` ≥99% (a team-season with all 4 cols non-null requires
the week-1 game's `spread_line` + `total_line` non-null in pbp). `season_avg_*`
~93-94% (week-1 rows are NaN by construction — ~6% of rows; the remainder is
the small `<1%` of subsequent weeks with missing pbp lines).

Observed: `preseason_*` at 100.00% exceeds the ≥99% target — every team-season
in 2018-2024 has a non-null week-1 spread + total in the source schedules. The
`season_avg_*` 94.13% lands inside the expected 93-94% bracket, and the NaN
share is exactly the week-1 rows (5.87% of total). Zero `season_avg_*` NaN
occurs on non-week-1 rows — no missing-line residual to investigate.

## Week-1 NaN rate on season_avg_*

- `season_avg_spread` week-1 NaN: 100.00% (expected — cold-start by construction).
- `season_avg_implied_team_total` week-1 NaN: 100.00%.

Confirmed at 100.00% / 100.00%. All 3,328 week-1 rows have NaN season-to-date
averages; all 53,324 non-week-1 rows are populated.

## Unique team-season counts

Counted as distinct `(preseason_spread, preseason_implied_team_total)` pairs
in week-1 rows per season — a stricter check than spread alone, because two
teams can coincidentally share an identical spread but different totals.

| Season | Unique (spread, ITT) pairs in week 1 |
|---|---|
| 2018 | 32 |
| 2019 | 30 |
| 2020 | 32 |
| 2021 | 30 |
| 2022 | 32 |
| 2023 | 30 |
| 2024 | 30 |

Expected 32 per season (one preseason value pair per team). Coverage on
`preseason_*` is 100%, so the dips to 30 in 2019/2021/2023/2024 are NOT
missing data — they are coincidental duplicate pairs (two teams whose week-1
game had identical `(spread_line, total_line)`, producing identical implied
team totals). Spread-alone counts collapse further; the (spread, ITT) pair
is the cleanest distinct-team proxy without joining a `team` column.

## Histogram bounds

| Column | min | max | mean |
|---|---:|---:|---:|
| `preseason_implied_team_total` | 15.50 | 31.50 | 22.85 |
| `preseason_spread` | -10.50 | 10.50 | -0.04 |
| `season_avg_implied_team_total` | 14.69 | 32.00 | 22.94 |
| `season_avg_spread` | -14.67 | 15.67 | -0.17 |

All within expected ranges:
- `implied_team_total` mean ~22-23 matches the average NFL team scoring
  environment (~46 total / 2).
- `preseason_spread` bounds at ±10.5 are plausible for week-1 (large mismatches
  are uncommon in week 1 before in-season ratings sharpen).
- `season_avg_spread` widens to -14.67/+15.67 because in-season averages can
  drift further for teams whose schedules cluster favorable or unfavorable.
- Both spread means are ~0 (-0.04 / -0.17), reflecting near-perfect sign
  symmetry across the dataset.

## Sign-convention sanity

Confirmed by inspecting unique `preseason_spread` values per season:

| Season | Negative (favored) values | Positive (dog) values | Zero |
|---|---:|---:|---:|
| 2018 | 10 | 10 | 0 |
| 2019 | 11 | 11 | 0 |
| 2020 | 10 | 10 | 0 |
| 2021 | 8 | 8 | 0 |
| 2022 | 9 | 9 | 0 |
| 2023 | 9 | 9 | 0 |
| 2024 | 9 | 9 | 0 |

Counts are distinct numeric spread values (not team counts). The perfect
neg/pos balance per season is the expected signature: every game produces
one favorite (negative spread) and one dog (positive spread), so the set of
distinct spread magnitudes is mirrored across zero. No zero spreads ("pick'em"
week-1 games) appeared in 2018-2024.

2023 unique spreads ranged from -9.5 (largest favorite) to +9.5 (largest dog),
with implied team totals ranging 15.5-28.5 — consistent with NFL week-1 priors.

## Notes / anomalies

No data-quality anomalies observed.

- 100% `preseason_*` coverage means no week-1 game in 2018-2024 had a missing
  `spread_line` or `total_line` in the source schedules — better than the
  ≥99% target.
- The 30-vs-32 dips in unique (spread, ITT) counts are coincidental ties on
  both spread and total, not missing data.
- `season_avg_*` NaN is exclusively week-1 cold-start (3,328/3,328); zero
  leakage on later weeks.
- Histogram bounds and mean ≈0 spreads confirm the file is ready for use in
  the family probe.
